# Generated by `npm run snapshot`. Do not edit.
#
# The URL contract's redirects, as literal rules: a static export runs no
# middleware, so every one of these is the web server's job.
#
# There is one rule today because there are no versions yet. Each snapshot adds
# the version-less path of the section it cut, pointing at its newest version.

location = / { return 302 /en/; }
