## What changes and why

<!-- One subject per pull request. If it closes an issue, write "Closes #123". -->

## How it was tested

<!-- The commands you ran, and the pages you read in the built site. -->

## Checklist

- [ ] `npm run lint`, `npm run typecheck`, and `npm run build` pass.
- [ ] Every `check:*` script that CI runs passes.
- [ ] A changed English page under `/client/` or `/install/` has its Polish page updated in the same pull request.
- [ ] The page describes what the code on `main` does, not a proposal.
